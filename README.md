# Лапы крылья и хвосты
## Описание проекта
## Развёртка
### Установка окружения
``` python
python -m virtualenv .venv
# Активируйте venv
pip install -r requirements.txt
```
### Запуск сервера
``` bash
fastapi dev --host 127.0.0.1 --port 8080
```
Реверс прокси по вкусу

